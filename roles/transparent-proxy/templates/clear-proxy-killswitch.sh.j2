#!/usr/bin/env bash
set -euo pipefail

if [[ "${EUID}" -ne 0 ]]; then
  echo "Run as root: sudo $0" >&2
  exit 1
fi

nft delete table inet transparent_proxy_killswitch >/dev/null 2>&1 || true
echo "Cleared experimental kill switch"
